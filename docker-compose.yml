version: '3.8'

services:
  lamlo-payslip:
    build: .
    container_name: lamlo-payslip-app
    restart: unless-stopped
    ports:
      # Diakses via http://localhost:8090 atau via Cloudflare Tunnel
      - "8090:80"
    environment:
      - TZ=Asia/Jakarta
