version: '3.8'

services:
  lamlo-payslip:
    build: .
    container_name: lamlo-payslip-app
    restart: unless-stopped
    ports:
      # Expose port 8080 di Ubuntu server untuk dihubungkan ke Cloudflare Tunnel
      - "8080:80"
    environment:
      - TZ=Asia/Jakarta
